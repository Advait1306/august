{
  "name": "zero-cache",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx zero-cache-dev",
    "docker:build": "docker build -t zero-image .",
    "docker:start": "docker rm -f zero >/dev/null 2>&1 || true && docker run -d --name zero --add-host=host.docker.internal:host-gateway --env-file ./.local.env -p 4848:4848 -v zero_data:/data zero-image"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@jupiter/sync": "*"
  }
}
